# Environment variables (API KEYS LIVE HERE — NEVER COMMIT)
.env
.env.local
.env.production
.env*.local

# Python
__pycache__/
*.pyc
venv/
.venv/

# Node
node_modules/
.next/

# Vector database (large, regenerated from PDFs)
chroma_db/

# OS files
.DS_Store
Thumbs.db

# PDF manuals (too large for Git — keep local)
data/manuals/*.pdf
